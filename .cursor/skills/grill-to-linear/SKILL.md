Distill this grill session into a German Linear project on the Engineering team: a clear project goal plus sharply cut issues—one responsibility each, concrete tasks, testable acceptance criteria, explicit out-of-scope, and real dependencies. Look up existing issue and project labels first and assign them by exact name (typically type + repo). Create project and issues in dependency order, then wire Linear relations (`blockedBy` / `blocks` for hard prerequisites, `relatedTo` for soft links); keep paths, commands, and service names in English where that reads naturally.
